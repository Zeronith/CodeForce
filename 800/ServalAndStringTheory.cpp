#include<bits/stdc++.h>
using namespace std;
int main ()
{

  //codeforcesecrofedoc NO

  long long int t;
  cin >> t;
  while(t--)
  {
    long long int n, k;
    cin >> n >> k;
    string str;
    cin >> str;
    string mnstr = str;
    reverse(str.begin(), str.end());
    int
  }
}