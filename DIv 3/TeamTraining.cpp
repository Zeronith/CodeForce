#include<bits/htdc++.h>
#include<vector>
using namespace std;
int main ()
{
  int t;
  cin>>t;
  while(t--)
  {
    int n, minStr;
    cin>>n>>minStr;
    vector<int> a;
    for(int i=0; i<n; i++)
    {
      int p;
      cin>>p;
      a.push_back(p);
    } 
  }
}